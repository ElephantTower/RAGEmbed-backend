model Model {
  id         String @id @default(cuid())
  nameInOllama String @unique
  queryPrefix      String @default("")
  documentPrefix   String @default("")

  embeddings Embedding[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}